# WORK-20260903-codex-icarus-4-pull: Puller: top-10 share, launchpad attribution, mint and LP checks, revenue series

Paste prompt for Codex (also the PR body of the kickoff PR).

```text
You are the Proofline engineer (producer codex) building Icarus on github.com/harsharn10/proofline. The spec is docs/design/icarus/README.md and the visual spec is docs/design/icarus/mock.html (open it in a browser; hash routes #/, #/s/launchpads, #/t/artificial-inu, #/n/pons). Read both before touching code, then docs/research-system.md for the content contract. Where the mock and README disagree, README wins.

work_id: WORK-20260903-codex-icarus-4-pull
producer: codex
branch: codex/20260903/WORK-20260903-codex-icarus-4-pull (already exists with this assignment file; commit to it, do not create another)
depends_on: none
allowed_paths:
  - scripts/pull.mjs
  - scripts/lib/pull/**
  - schema/pulled.schema.json
  - scripts/test-pull.mjs + test fixtures
  - site/src/data/types.ts (Pulled* types only)
  - content/pulled/** (regenerated by one real run of npm run pull, committed separately)
  - docs/integrations/pull.md (new or existing)
  - .github/workflows/pull.yml (timeout only if needed)

Rules that always apply (README §3): reader words only, every number links to its source, the share bar, no wallet labeling, charts from snapshots only, honest placeholders, build stays green. Do not change files outside allowed_paths; if you must, stop and say why in the PR.

Before you push: npm test, npm run validate:release (0 errors), npm --prefix site run test, npm --prefix site run build all pass. Commit in small steps with messages ending in the trailer "Producer: codex". Push to the branch and mark the PR ready with the body:
## Done
- one line per change
## Screens
- attach screenshots of every changed page in light and dark (mobile width too)
## Not done / questions
- anything left, with the reason
Never merge, never enable auto-merge. A controller reviews with docs/design/icarus/review-checklist.md.

Task: extend scripts/pull.mjs and its libs with the reads the cards need (README §5). Free, keyless sources only: Blockscout API v2 at https://robinhoodchain.blockscout.com/api/v2 (send a browser User-Agent; about 2 s per page; cap pages), DexScreener https://api.dexscreener.com/token-pairs/v1/robinhood/<token>, public RPC https://rpc.mainnet.chain.robinhood.com, DefiLlama https://api.llama.fi. Every new field is nullable with an errors[] reason when not determinable; never guess.

1. Top-10 share (tokens): GET /tokens/<addr>/holders page 1 and /tokens/<addr> total_supply → market.top10_share (0–1) and market.top10_share_ex_pools (excluding the pair addresses in market.pairs and any address whose pulled role is vault or locker), with top10_as_of.
2. Launchpad attribution (tokens): GET /addresses/<addr> → creator_address_hash and creation_tx_hash; match the creator against every factory- or curve-role address across content/pulled/*.yaml and the known launcher deployers → market.launchpad {slug, via: "factory" | "creator", address} else null.
3. Mint and renounce (tokens): from the verified ABI (GET /smart-contracts/<addr>) → structure.mint = "owner-can-mint" when a mint function exists and owner() is non-zero, "no-mint-function", or "unknown" (unverified source); structure.renounced = owner() is the zero address.
4. LP lock (tokens): for each pair that is an ERC-20 (Uniswap v2 style) read the LP token's top holders → structure.lp[] {pair, locked_share (share held by burn addresses 0x0/0xdead or by addresses whose pulled role is locker), holder_kind}; v3/v4 pools are positions, record null with reason "v3/v4 position; not checked".
5. Revenue (protocols with a DefiLlama receipt): GET /summary/fees/<protocol>?dataType=dailyRevenue → metrics.revenue_24h and content/pulled/series/<slug>.json {revenue_daily: [[YYYY-MM-DD, usd]...] last 90 days}; chain-slice to Robinhood Chain when the response is per-chain.
6. Snapshot lines in content/pulled/history/<slug>.jsonl gain revenue_24h and top10_share. History stays append-only.
7. Add --only <slug,slug> to pull.mjs if missing; keep a full run inside the workflow's 60-minute budget (measure and print per-slug timing in --dry).
8. schema/pulled.schema.json, Pulled* types and scripts/test-pull.mjs fixtures updated; docs page lists every field, its source URL pattern and its refresh cadence.
Acceptance: npm run test:pull passes; npm run pull -- --only pons,artificial-inu writes the new blocks; a full npm run pull completes and validates; the PR lists per-field coverage (how many of the 34 located names got each field).
```
